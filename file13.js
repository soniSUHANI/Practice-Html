// metchod chaining = calling one method after another
//                    in one continuous line of code

let userName = "soni";


let letter = userName.charAt(0).toUpperCase().trim();
//letter = letter.toUpperCase();

console.log(letter);
